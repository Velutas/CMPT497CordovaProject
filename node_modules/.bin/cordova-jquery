#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../cordova-jquery/cordova-jquery" "$@"
  ret=$?
else 
  node  "$basedir/../cordova-jquery/cordova-jquery" "$@"
  ret=$?
fi
exit $ret
